//para poder imprimir en la consola, el preprocesador incluye las funcionalidades la biblioteca stdio.h, 
//donde se encuentran las funciones necesarias para entrada y salida estandar
#include <stdio.h>
//la funcion main;donde se inicia la ejecuci´on del programa
int main(){
	//llamo a la funcion printf para imprimir en la consola
	printf("¡Hola, mundo!");
	//regresa 0 para indicar que el programa acabo de manera correcta; estado de salida del programa
	return 0;
}